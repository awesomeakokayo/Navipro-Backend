services:
  - type: web
    name: navipro-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -k uvicorn.workers.UvicornWorker agent_orchestra:app --bind 0.0.0.0:$PORT"
    runtime: "python-3.11"
